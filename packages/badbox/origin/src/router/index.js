import Vue from 'vue'
import Router from 'vue-router'
// import badBox from '@/components/badBox'

Vue.use(Router)

export default new Router({
  routes: [
    {
      path: '/',
      name: 'badBox',
      // component: badBox
    }
  ]
})
